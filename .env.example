# Atlas Monorepo — Master .env Reference
# Each sub-package has its own .env.example with service-specific variables.
#
# frontend/   → frontend/.env.example    → Vercel
# backend/    → backend/.env.example     → Render (Docker)
# agents/     → agents/.env.example      → (imported by backend, or run as worker)
